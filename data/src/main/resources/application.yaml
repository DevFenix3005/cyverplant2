spring:
  application.name: Data
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USER}
    password: ${DATABASE_PASSWORD}
    driver-class-name: org.postgresql.Driver
server:
  port: 8081
api:
  base-path: /api/v1
okta:
  oauth2:
    issuer: https://devfenix3005.us.auth0.com
    audience: https://cyverplant/api
